{
  "name": "@solaros/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"lint configured in T0.1.2\" && exit 0",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf node_modules .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "rimraf": "^6.0.0"
  }
}
